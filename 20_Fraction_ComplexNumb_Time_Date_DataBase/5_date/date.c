#include "date.h"



Date CheckDate(Date date);

void Input(Data *date)
{

}


void Output(Data date);
void AddDays(Data *date, int amount_of_days);
int DifferenceBetweenDates(Date date_1, Date date_2);

Date CheckDate(Date date)
{
    if(date.year % 400 == NULL)
    {
        if(date.year % 100 = NULL)
        {
            if()
        }
    }
}
